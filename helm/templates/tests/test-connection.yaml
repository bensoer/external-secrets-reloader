apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "external-secrets-reloader.fullname" . }}-test-connection"
  labels:
    {{- include "external-secrets-reloader.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "external-secrets-reloader.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
